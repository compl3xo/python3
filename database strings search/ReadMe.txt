This tool is used for searching for certain string, you could just ctrl+f, but sometimes some people have computers that can't open big text files, so this script helps with that. 

You need to make sure your database dump is is in the source code. Replace "example.txt", with your file. 

REQUIREMENTS {
(H)ave python3 installed
(M)odules:
Colorama
}
